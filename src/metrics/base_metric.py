# Phase 2: Deprecated. Do not use.
# All metrics must inherit from `Metric` (src/metrics/metric.py).

# Keeping this stub so old imports don't crash while we migrate tests.
class BaseMetric:  # pragma: no cover
    pass